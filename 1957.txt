1957
My Solution:
var makeFancyString = function(s) {
    const r = s.split('')
    r.forEach((ch, i) => {
        if(ch === s[i-1] && ch === s[i+1]) {
            r[i] = '';
        }
    });
    return r.join('')
};

Best Solution:
return s.replace(/(.)\1{2,}/g, '$1$1');